import { TeachersSection } from "@/features/teachers/TeachersSection";

export default function Page() {
  return (
    <main className="bg-white text-slate-900">
      <TeachersSection headingLevel={1} />
    </main>
  );
}
