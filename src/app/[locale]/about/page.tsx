import { AboutSection } from "@/features/about/AboutSection";

export default function Page() {
  return (
    <main className="bg-surface text-ink-900">
      <AboutSection headingLevel={1} />
    </main>
  );
}
