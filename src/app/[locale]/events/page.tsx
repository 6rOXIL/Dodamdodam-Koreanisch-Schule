import { EventsSection } from "@/features/events/EventsSection";

export default function Page() {
  return (
    <main className="bg-white text-slate-900">
      <EventsSection headingLevel={1} />
    </main>
  );
}
