import { LocationSection } from "@/features/location/LocationSection";

export default function Page() {
  return (
    <main className="bg-white text-slate-900">
      <LocationSection headingLevel={1} />
    </main>
  );
}
